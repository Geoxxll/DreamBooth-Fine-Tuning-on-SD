# Vendored training script

`train_dreambooth_lora.py` is copied from the Hugging Face **diffusers** repository:

- **Tag:** `v0.31.0`
- **Path in upstream:** `examples/dreambooth/train_dreambooth_lora.py`
- **Source:** [github.com/huggingface/diffusers](https://github.com/huggingface/diffusers) (Apache 2.0)

The installed package version in `requirements.txt` must satisfy `check_min_version("0.31.0")` inside the script.

To refresh from upstream (after changing the pin in `requirements.txt`):

```bash
curl -fsSL -o scripts/train_dreambooth_lora.py \
  "https://raw.githubusercontent.com/huggingface/diffusers/v0.31.0/examples/dreambooth/train_dreambooth_lora.py"
```
